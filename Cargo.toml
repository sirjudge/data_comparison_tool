[package]
name = "data_comparison_tool"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
async-std = "1.12.0"
block-on = "0.3.0"
sqlx = { version = "0.7.3", features = [ "runtime-tokio", "tls-native-tls" , "sqlite", "mysql", "time"  ] }
